# --- Application Configuration ---
spring.application.name=com.movie.app
# --- Server Configuration ---
server.port=8081
# Tomcat tuning for large file uploads
server.tomcat.max-swallow-size=-1
server.tomcat.max-http-form-post-size=-1
# --- Database Configuration (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5433/movie_app
spring.datasource.username=postgres
spring.datasource.password=postgres
# --- JPA / Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# --- File Upload Configuration ---
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=2GB
spring.servlet.multipart.max-request-size=2GB
# --- Mail Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.starttls.enabled=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
# --- Security / JWT Configuration ---
secret.jwt.string=b2Lg1fPqs92hJ3nT1VpXw!Gz0zPb3kXv7YvX@jJs9R8tT0h8K9V5e8b2v!hC
# --- Custom File Paths ---
file.upload.video-dir=uploads/videos
file.upload.image-dir=uploads/images
# --- CORS / Frontend ---
app.frontend.url=http://localhost:4200